spring:
  application:
    name: reminder-bot
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 1
      auto-commit: false
  jpa:
    show-sql: ${SHOW_SQL:true}
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.default_schema: ${POSTGRES_SCHEMA:reminderbot}
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: true
    default-schema: reminderbot
    change-log: classpath:/liquibase/master.yml
    database-change-log-table: _databasechangelog
    database-change-log-lock-table: _databasechangeloglock
bot:
  name: "NvdReminderBot"
  token: 7022755394:AAH46I2mqS0svl888uNqUT9e49hHMWU6SCM